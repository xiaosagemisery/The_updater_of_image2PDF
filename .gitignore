.idea/
__pycache__/
*.py[cod]
.pytest_cache/
webdata/

# PyInstaller 打包产物: build/dist 是本地/CI 每次重新生成的中间产物和最终 exe,
# 不进仓库(exe 通过 GitHub Release 分发,不通过 git 提交)。.spec 文件本身要提交,
# 它是打包配置,不是产物。
build/
dist/
*.exe
